git init

git status

git add index.js
git add . the whole folder

git commit -m 'initial commit'

git push -u origin main

---

CLONE REPO
code ssh copy

git clone paste address

Back to last commit
git restore .
